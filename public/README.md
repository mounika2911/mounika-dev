Place your resume PDF in this folder as `resume.pdf`.

In a Vite app, files in `public/` are served at the site root, so you can reference:

- `/resume.pdf`

Add your actual resume file here and then use that path in your portfolio.